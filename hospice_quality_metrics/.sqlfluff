[sqlfluff]
dialect = duckdb  # or the dialect you use, e.g., 'postgres'
templater = dbt
exclude_rules = AM04, LT05, RF05 
ignore = templating

[sqlfluff:templater:dbt]
project_dir = .  # Path to your dbt project